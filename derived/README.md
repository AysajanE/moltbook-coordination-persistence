# derived/

Tracked derived tables and sanitized analytic artifacts produced from canonical frozen archives.

- Derived tables must follow the schema contracts in `contracts/schemas/`.
- Do not place raw archive payloads or restricted mappings here.
