# Internal Document Notice

This path is reserved for the detailed theory and methods packet used in the private research workspace for this project.

The full packet is intentionally omitted from the public repository. This public stub preserves path compatibility for contracts, runbooks, and tooling that refer to the authority surface by filename.

Public readers should use:

- `README.md` for the project overview
- `contracts/project.yaml` and `contracts/framework.json` for the public runtime structure
- the public runbooks under `docs/` for workflow context
