# `integration_ready/`

Optional integration checkpoint.

Use this state/folder for tasks that have **exported interfaces/contracts** needed for downstream work, so dependent tasks can proceed before final review/closure.

